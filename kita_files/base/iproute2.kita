KITA_TYPE="make"
FILES="http://developer.osdl.org/dev/iproute2/download/iproute2-2.6.23.tar.bz2"
DEPEND=""
BUILD=""
config_src(){
echo "nothing"
}

build_src(){
make SBINDIR=/sbin
}

kita_install(){
mkdir -p $INSTALL_DIR/sbin
make SBINDIR=$INSTALL_DIR/sbin install
#mv $INSTALL_DIR/* $INSTALL_DIR/sbin/
}

post_install(){
mv -v /sbin/arpd /usr/sbin
}
""

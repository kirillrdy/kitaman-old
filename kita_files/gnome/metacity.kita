KITA_TYPE="gnome"
DEPEND="gtk+ XML-Parser zenity"
BUILD=""
config_src()
{
./configure \
    --prefix=/usr \
    --libexecdir=/usr/lib/metacity \
    --sysconfdir=/etc \
    --with-gconf-schema-file-dir=/etc/gconf/schemas
}
""

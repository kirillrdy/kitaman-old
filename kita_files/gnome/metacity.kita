KITA_TYPE="make"
DEPEND="gtk+ XML-Parser"
BUILD=""
build_src()
{
./configure \
    --prefix=/usr \
    --libexecdir=/usr/lib/metacity \
    --sysconfdir=/etc \
    --with-gconf-schema-file-dir=/etc/gconf/schemas &&
make
}
""

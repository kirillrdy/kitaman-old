# hello
FAKE=""
KITA_VER="1.0"
FILES="http://www.ijg.org/files/jpegsrc.v6b.tar.gz"
DEPEND="glib"
BUILD=""
build_src(){
./configure --prefix=/usr --enable-static --enable-shared &&
make
}
kita_install()
{
mkdir -p $INSTALL_DIR/usr/bin
mkdir -p $INSTALL_DIR/usr/lib
mkdir -p $INSTALL_DIR/usr/man/man1
mkdir -p $INSTALL_DIR/usr/include
make prefix=$INSTALL_DIR/usr bindir=$INSTALL_DIR/usr/bin libdir=$INSTALL_DIR/usr/lib includedir=$INSTALL_DIR/usr/include install
}
""
